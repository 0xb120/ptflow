"""Real external web pipeline (ProjectDiscovery toolchain). Port of scope2surface.sh.

This round implements only the asset_discovery (breadth) phase. Clustering and
per-app enumeration are added in later rounds.
"""
