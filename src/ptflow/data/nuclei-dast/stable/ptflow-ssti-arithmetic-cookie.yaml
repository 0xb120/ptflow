id: ptflow-ssti-arithmetic-cookie

info:
  name: SSTI Arithmetic Evaluation via Cookie
  author: ptflow
  severity: high
  description: Detects server-side template evaluation from a cookie value using an exact arithmetic result.
  metadata:
    max-request: 3
    verified: true
  tags: ptflow,dast,ssti,stable,fuzzing-req-cookie

http:
  -
    pre-condition:
      - type: dsl
        dsl:
          - 'method != "OPTIONS"'

    payloads:
      expression:
        low:
          - '{{1337*7331}}'
          - '<%= 1337*7331 %>'
          - '#{1337*7331}'

    fuzzing:
      - part: cookie
        mode: single
        type: replace
        fuzz:
          - '{{expression}}'

    stop-at-first-match: true
    matchers:
      - type: word
        part: body
        words:
          - '9801547'
